{
  "name": "energy-mail",
  "productName": "Energy Mail",
  "description": "E-Mail-Programm für beliebige IMAP/SMTP-Anbieter",
  "author": "Hendrik Zeuch",
  "//repository": "Aus diesem Eintrag ermittelt electron-builder, wohin veroeffentlicht wird und wo die ausgelieferte Anwendung nach Aktualisierungen sucht (er landet als app-update.yml im Paket). Wird die Adresse geaendert, muessen bereits installierte Fassungen einmal von Hand ersetzt werden - sie suchen sonst weiter an der alten Stelle.",
  "repository": {
    "type": "git",
    "url": "https://github.com/EnergyTrading07/energy-mail.git"
  },
  "private": true,
  "version": "0.2.1",
  "license": "MIT",
  "main": "packages/desktop/dist/main.js",
  "//type": "Muss gesetzt sein, sonst startet die paketierte Anwendung nicht: Electron entscheidet anhand DIESER Datei, ob der Einstiegspunkt als ES-Modul oder als CommonJS geladen wird - nicht anhand der package.json daneben. Aus dem Quellbaum gestartet ist packages/desktop das Wurzelverzeichnis (dort steht type bereits), paketiert dagegen dieses hier. Ohne die Angabe scheitert der Start mit ERR_REQUIRE_ESM.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npm run build && npm run start --workspace packages/desktop",
    "//test": "Der Laeufer von Node findet die Pruefdateien selbst. Vorher standen hier 33 tsx-Aufrufe mit && aneinandergereiht, mit vier Nachteilen: der Lauf brach beim ersten Fehler ab (man sah immer nur den ersten von moeglicherweise fuenf kaputten), es gab keine Gesamtzusammenfassung, jede neue Pruefdatei musste von Hand eingetragen werden - wurde sie vergessen, lief sie nie und niemand merkte es - und alles lief nacheinander. Gemessen: 30 s vorher, 5 s jetzt. tsx bleibt noetig, aber als Lader: Node entfernt Typen zwar selbst, loest aber ./x.js nicht auf ./x.ts auf.",
    "test": "node --import tsx --test \"packages/*/src/**/*.test.mts\"",
    "test:abdeckung": "node --import tsx --test --experimental-test-coverage \"packages/*/src/**/*.test.mts\"",
    "//pruefe": "Was die CI macht, in einem Aufruf - damit sich lokal derselbe Stand pruefen laesst, ohne drei Befehle zu merken.",
    "pruefe": "npm run typecheck && npm run build && npm test",
    "build": "npm run build -w packages/mail-core && npm run build -w packages/server && npm run build -w packages/web && npm run build -w packages/desktop",
    "typecheck": "npm run build -w packages/mail-core && npm run build -w packages/server && npm run typecheck --workspaces --if-present",
    "dist": "npm run build && electron-builder",
    "//veroeffentlichen": "Prueft den Stand und setzt die Marke - mehr nicht. Gebaut und hochgeladen wird danach in der CI (.github/workflows/veroeffentlichen.yml). Vorher lief beides hier: die ausgelieferte Datei war damit das Ergebnis eines Rechners, dessen Zustand niemand nachvollziehen kann, und bei einer Anwendung mit Selbstaktualisierung ist der Bauplatz die empfindlichste Stelle der Kette. Es wird kein GitHub-Schluessel mehr gebraucht - gepusht wird ueber die gewoehnlichen Git-Zugangsdaten, und den Schluessel zum Hochladen stellt GitHub in der CI selbst.",
    "veroeffentlichen": "node scripts/pruefe-veroeffentlichung.mjs && node scripts/marke-setzen.mjs",
    "//publish": "Alter Name, damit die Gewohnheit nicht ins Leere laeuft.",
    "publish": "npm run veroeffentlichen",
    "dev:server": "npm run dev --workspace packages/server",
    "dev:web": "npm run dev --workspace packages/web",
    "dev:desktop": "npm run dev --workspace packages/desktop"
  },
  "dependencies": {
    "@energy-mail/server": "*",
    "@energy-mail/web": "*",
    "electron-updater": "^6.8.9"
  },
  "devDependencies": {
    "@types/jsdom": "^30.0.0",
    "electron": "^43.3.0",
    "electron-builder": "^26.15.3",
    "jsdom": "^29.1.1",
    "tsx": "^4.23.11",
    "typescript": "^5.6.3"
  }
}
